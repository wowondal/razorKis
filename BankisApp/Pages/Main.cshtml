@page
@model MainModel
@{
    ViewData["Title"] = "메인 페이지";
}

<h2>메인 페이지</h2>
<p>@Model.Username 님, 환영합니다.</p>

<form method="post" asp-page-handler="Logout">
    <button type="submit">로그아웃</button>
</form>